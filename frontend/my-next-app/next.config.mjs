/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["readymadeui.com", "imgs.search.brave.com", "trustwallet.com"],
  },
};

export default nextConfig;